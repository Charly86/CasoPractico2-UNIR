apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: game-ingress
  annotations:
    kubernetes.io/ingress.class: "nginx"
spec:
  rules:
  - host: cvillar.unir.cp2
    http:
      paths:
      - path: /game
        pathType: Prefix
        backend:
          service: 
            name: game-service
            port:
              number: 80