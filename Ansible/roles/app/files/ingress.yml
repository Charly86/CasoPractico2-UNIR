apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: nginx-ingress
spec:
  rules:
  - host: vm-masterNFS
    http:
      paths:
      - path: /app_example
        backend:
          serviceName: nginx-service
          servicePort: 8081